Can have diff .env files. .env.local, .production, .developement.

next.config.js 

state is not preserved in Client Components (?)

// export async function POST(request: Request) {}

// type Params = {
//   team: string;
// };

// export async function GET(request: Request, context: { params: Params }) {
//   const team = context.params.team; // '1'
// }

// Define params type according to your route parameters (see table below)

// Store whole user state in redux. When user state changes, you need to update it in both redux and firebase auth.